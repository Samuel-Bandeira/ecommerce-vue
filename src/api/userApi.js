export const addProductToCart = ({ item }) => {
  console.log(item);
  //salvar alteração no banco
};

export const mergeCart = () => {
  //pegar o cart do banco
  //fazer a adição dos items do store
  //salvar alteração no banco
};
